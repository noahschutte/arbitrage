import React from 'react';
import { Link } from 'react-router-dom';

const NotFoundPage = () => (
  <div>
    Nothing to see here. Would you like to go back to the: <Link to="/">Dashboard</Link>
  </div>
);

export default NotFoundPage;
